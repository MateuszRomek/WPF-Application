<Page x:Class="WPFProject.Views.ListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFProject.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:i1="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      Title="ListView">


    <Grid>

        <DataGrid>
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="Loaded">
                    <i:InvokeCommandAction Command="{Binding LoadData}" />
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </DataGrid>

        <ListView ItemsSource="{Binding Movies}">
            <ListView.View>
                <GridView>
                    <GridView.ColumnHeaderContainerStyle>
                        <Style TargetType="{x:Type GridViewColumnHeader}">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Setter Property="TextElement.FontSize" Value="16" />
                            <Setter Property="HorizontalContentAlignment" Value="Left" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="Padding" Value="5,3,3,3"/>
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="TextElement.Foreground" Value="Black"/>
                                </Trigger>
                            </Style.Triggers>
                            
                            
                            
                        </Style>

                    </GridView.ColumnHeaderContainerStyle>
                    <GridViewColumn  Header="Tytuł Filmu" Width="270" DisplayMemberBinding="{Binding MovieTitle}" />
                    <GridViewColumn Header="Obejrzany na" Width="270" DisplayMemberBinding="{Binding PlatformName}" />
                    <GridViewColumn Header="Gatunek" Width="270" DisplayMemberBinding="{Binding MovieGenre}" />
                    <GridViewColumn Header="Ocena w skali 1 - 5" Width="270" DisplayMemberBinding="{Binding MovieRating}" />
                </GridView>
            </ListView.View>
        </ListView>


    </Grid>
</Page>
